Add screenshots of the running app here for the README, e.g.:

- home.png       — landing page hero
- predict.png    — prediction form
- result.png     — prediction result with gauge + risk meter
- dashboard.png  — model performance dashboard

Run the app locally (`python app.py`) and capture these at ~1440px width for best results.
